openapi: 3.0.4
info:
  title: Assets API
  description: |
    API for reading and creating assets.

    Authentication is performed using an API key associated with an
    active user. The user's RBAC permissions and client access are
    enforced server-side.

    The API uses GET for read operations and POST for create operations.
    API responses use a success field containing the string "True" or "False".
  version: 1.0.0
servers:
  - url: https://develop.dev.itflow.org/api/v1
    description: Development API server - api_key is 'develop'
tags:
  - name: Assets
    description: Asset management operations
paths:
  /assets/read.php:
    get:
      tags:
        - Assets
      summary: Read assets
      description: |
        Retrieves assets accessible to the authenticated API user.

        Supports several mutually exclusive lookup parameters.
        The first matching parameter in the following order is used:

        1. client_id
        2. asset_id
        3. asset_type
        4. asset_name
        5. asset_serial
        6. asset_mac
        7. asset_uri
        8. asset_uri_2

        If none of these parameters are supplied, the endpoint returns
        a paginated list of all assets accessible to the authenticated user.
      operationId: readAssets
      security:
        - ApiKeyAuth: []
      parameters:
        - name: client_id
          in: query
          required: false
          description: |
            Optional client ID to scope the read to a specific client.
            This is only used when the authenticated API key has access
            to that client.
          schema:
            type: integer
            minimum: 1
          example: 1
        - name: asset_id
          in: query
          required: false
          description: Retrieve a single asset by its ID.
          schema:
            type: integer
            format: int64
            minimum: 1
          example: 123
        - name: asset_type
          in: query
          required: false
          description: Retrieve assets matching the specified asset type.
          schema:
            type: string
          example: Server
        - name: asset_name
          in: query
          required: false
          description: Retrieve assets matching the exact asset name.
          schema:
            type: string
          example: Server01
        - name: asset_serial
          in: query
          required: false
          description: Retrieve assets matching the exact serial number.
          schema:
            type: string
          example: ABC123456
        - name: asset_mac
          in: query
          required: false
          description: |
            Retrieve assets by the MAC address of their primary interface.
          schema:
            type: string
          example: '00:11:22:33:44:55'
        - name: asset_uri
          in: query
          required: false
          description: Retrieve assets matching the specified URI.
          schema:
            type: string
          example: https://server01.example.com
        - name: asset_uri_2
          in: query
          required: false
          description: Retrieve assets matching the secondary URI.
          schema:
            type: string
          example: https://server01-alt.example.com
        - name: limit
          in: query
          required: false
          description: |
            Maximum number of records returned for collection queries.
            Defaults to 50.
          schema:
            type: integer
            minimum: 0
            default: 50
          example: 50
        - name: offset
          in: query
          required: false
          description: |
            Number of records to skip for collection queries.
            Defaults to 0.
          schema:
            type: integer
            minimum: 0
            default: 0
          example: 0
      responses:
        '200':
          description: |
            Request processed successfully. The response may contain
            matching resources or indicate that no resources were found.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ReadResponse'
              examples:
                assetsFound:
                  summary: Assets found
                  value:
                    success: 'True'
                    count: 1
                    data:
                      - asset_id: 123
                        asset_name: Server01
                        asset_description: Production server
                        asset_type: Server
                        asset_make: Dell
                        asset_model: PowerEdge R650
                        asset_serial: ABC123456
                        asset_os: Ubuntu 24.04
                        asset_uri: https://server01.example.com
                        asset_uri_2: ''
                        asset_status: Active
                        asset_location_id: 5
                        asset_vendor_id: 10
                        asset_contact_id: 25
                        asset_purchase_date: '2026-01-15'
                        asset_warranty_expire: '2029-01-15'
                        asset_install_date: '2026-01-20'
                        asset_notes: Production server
                        asset_client_id: 1
                        interface_id: 456
                        interface_name: '1'
                        interface_mac: '00:11:22:33:44:55'
                        interface_ip: 192.168.1.10
                        interface_primary: 1
                        interface_network_id: 3
                        interface_asset_id: 123
                noAssetsFound:
                  summary: No matching assets
                  value:
                    success: 'False'
                    message: >-
                      No resource (for this client and company) with the
                      specified parameter(s).
        '401':
          description: Authentication failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
              example:
                success: 'False'
                message: Authentication failed. API key is invalid or has expired.
        '403':
          description: |
            The authenticated user does not have permission to read assets
            or does not have access to the relevant client.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
              examples:
                permissionDenied:
                  summary: Insufficient permission
                  value:
                    success: 'False'
                    message: >-
                      The user linked to this API key does not have permission
                      for this action.
                resourceDenied:
                  summary: Resource not mapped
                  value:
                    success: 'False'
                    message: This API resource is not mapped to a permission module.
        '405':
          description: Method not allowed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
  /assets/create.php:
    post:
      tags:
        - Assets
      summary: Create an asset
      description: |
        Creates a new asset for a client.

        Also creates a primary interface for the asset using
        asset_ip, asset_mac and asset_network_id.
      operationId: createAsset
      security:
        - ApiKeyAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CreateAssetRequest'
            example:
              client_id: 1
              asset_name: Server01
              asset_description: Production server
              asset_type: Server
              asset_make: Dell
              asset_model: PowerEdge R650
              asset_serial: ABC123456
              asset_os: Ubuntu 24.04
              asset_ip: 192.168.1.10
              asset_mac: '00:11:22:33:44:55'
              asset_uri: https://server01.example.com
              asset_uri_2: ''
              asset_status: Active
              asset_purchase_date: '2026-01-15'
              asset_warranty_expire: '2029-01-15'
              asset_install_date: '2026-01-20'
              asset_notes: Production server
              asset_vendor_id: 10
              asset_location_id: 5
              asset_contact_id: 25
              asset_network_id: 3
      responses:
        '200':
          description: |
            The request was processed. The PHP implementation returns
            a success or failure object from create_output.php.
          content:
            application/json:
              schema:
                oneOf:
                  - $ref: '#/components/schemas/CreateSuccessResponse'
                  - $ref: '#/components/schemas/ErrorResponse'
              examples:
                created:
                  summary: Asset created
                  value:
                    success: 'True'
                    count: '1'
                    data:
                      - insert_id: 123
                insertFailed:
                  summary: Insert failed
                  value:
                    success: 'False'
                    message: >-
                      Auth success but insert query failed, ensure ALL required
                      variables are provided (and aren't duplicates where
                      applicable) and database schema is up-to-date. Turn on
                      error logging and look for 'undefined index'.
        '401':
          description: Authentication failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
              example:
                success: 'False'
                message: Authentication failed. API key is invalid or has expired.
        '403':
          description: |
            The authenticated user does not have permission to create
            assets or does not have access to the target client.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
              examples:
                permissionDenied:
                  summary: Insufficient permission
                  value:
                    success: 'False'
                    message: >-
                      The user linked to this API key does not have permission
                      for this action.
                clientDenied:
                  summary: Client access denied
                  value:
                    success: 'False'
                    message: >-
                      The user linked to this API key does not have access to
                      the target client for this write.
        '405':
          description: Method not allowed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
              example:
                success: 'False'
                message: Can only send POST requests to this endpoint.
  /assets/update.php:
    post:
      tags:
        - Assets
      summary: Update an asset
      description: |
        Updates an existing asset for the authenticated client.

        Requires asset_id. Any field omitted from the request
        is treated as the current database value.
      operationId: updateAsset
      security:
        - ApiKeyAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UpdateAssetRequest'
            example:
              client_id: 1
              asset_id: 123
              asset_name: Server01
              asset_description: Production server
              asset_type: Server
              asset_make: Dell
              asset_model: PowerEdge R650
              asset_serial: ABC123456
              asset_os: Ubuntu 24.04
              asset_ip: 192.168.1.10
              asset_mac: '00:11:22:33:44:55'
              asset_uri: https://server01.example.com
              asset_uri_2: ''
              asset_status: Active
              asset_purchase_date: '2026-01-15'
              asset_warranty_expire: '2029-01-15'
              asset_install_date: '2026-01-20'
              asset_notes: Production server
              asset_vendor_id: 10
              asset_location_id: 5
              asset_contact_id: 25
              asset_network_id: 3
      responses:
        '200':
          description: |
            The request was processed. The PHP implementation returns
            a success or failure object from update_output.php.
          content:
            application/json:
              schema:
                oneOf:
                  - $ref: '#/components/schemas/UpdateSuccessResponse'
                  - $ref: '#/components/schemas/ErrorResponse'
              examples:
                updated:
                  summary: Asset updated
                  value:
                    success: 'True'
                    count: 1
                    data:
                      - asset_id: 123
                updateFailed:
                  summary: Update failed
                  value:
                    success: 'False'
                    message: >-
                      Auth success but update query failed, ensure ALL required
                      variables are provided (and aren't duplicates where
                      applicable) and database schema is up-to-date.
        '401':
          description: Authentication failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
              example:
                success: 'False'
                message: Authentication failed. API key is invalid or has expired.
        '403':
          description: |
            The authenticated user does not have permission to update
            assets or does not have access to the target client.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
              examples:
                permissionDenied:
                  summary: Insufficient permission
                  value:
                    success: 'False'
                    message: >-
                      The user linked to this API key does not have permission
                      for this action.
                clientDenied:
                  summary: Client access denied
                  value:
                    success: 'False'
                    message: >-
                      The user linked to this API key does not have access to
                      the target client for this write.
        '405':
          description: Method not allowed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
              example:
                success: 'False'
                message: Can only send POST requests to this endpoint.
  /assets/delete.php:
    post:
      tags:
        - Assets
      summary: Delete an asset
      description: |
        Deletes an existing asset for the authenticated client.

        Requires asset_id. The asset and its associated interfaces are
        removed when the delete succeeds.
      operationId: deleteAsset
      security:
        - ApiKeyAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DeleteAssetRequest'
            example:
              client_id: 1
              asset_id: 123
      responses:
        '200':
          description: |
            The request was processed. The PHP implementation returns
            either a successful delete payload or a logical failure payload,
            while still using HTTP 200 OK.
          content:
            application/json:
              schema:
                oneOf:
                  - $ref: '#/components/schemas/DeleteSuccessResponse'
                  - $ref: '#/components/schemas/ErrorResponse'
              examples:
                deleted:
                  summary: Asset deleted
                  value:
                    success: 'True'
                    count: 1
                    data:
                      - asset_id: 123
                deleteFailed:
                  summary: Delete failed
                  value:
                    success: 'False'
                    message: >-
                      Auth success but delete query failed, ensure ALL required
                      variables are provided and database schema is up-to-date.
        '401':
          description: Authentication failed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
              example:
                success: 'False'
                message: Authentication failed. API key is invalid or has expired.
        '403':
          description: |
            The authenticated user does not have permission to delete
            assets or does not have access to the target client.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
              examples:
                permissionDenied:
                  summary: Insufficient permission
                  value:
                    success: 'False'
                    message: >-
                      The user linked to this API key does not have permission
                      for this action.
                clientDenied:
                  summary: Client access denied
                  value:
                    success: 'False'
                    message: >-
                      The user linked to this API key does not have access to
                      the target client for this write.
        '405':
          description: Method not allowed.
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ErrorResponse'
              example:
                success: 'False'
                message: Can only send POST requests to this endpoint.
components:
  securitySchemes:
    ApiKeyAuth:
      type: apiKey
      in: query
      name: api_key
  schemas:
    ErrorResponse:
      type: object
      properties:
        success:
          type: string
          example: 'False'
        message:
          type: string
          example: Authentication failed. API key is invalid or has expired.
        count:
          type: integer
          nullable: true
          example: null
        data:
          type: array
          nullable: true
          items: {}
    ReadResponse:
      type: object
      properties:
        success:
          type: string
          example: 'True'
        count:
          type: integer
          example: 1
        message:
          type: string
          nullable: true
          example: null
        data:
          type: array
          items:
            $ref: '#/components/schemas/AssetRecord'
    CreateSuccessResponse:
      type: object
      properties:
        success:
          type: string
          example: 'True'
        count:
          type: string
          example: '1'
        data:
          type: array
          items:
            type: object
            properties:
              insert_id:
                type: integer
                example: 123
    UpdateSuccessResponse:
      type: object
      properties:
        success:
          type: string
          example: 'True'
        count:
          type: integer
          example: 1
        data:
          type: array
          items:
            type: object
            properties:
              asset_id:
                type: integer
                example: 123
    DeleteSuccessResponse:
      type: object
      properties:
        success:
          type: string
          example: 'True'
        count:
          type: integer
          example: 1
        data:
          type: array
          items:
            type: object
            properties:
              asset_id:
                type: integer
                example: 123
    AssetRecord:
      type: object
      properties:
        asset_id:
          type: integer
          example: 123
        asset_name:
          type: string
          example: Server01
        asset_description:
          type: string
          nullable: true
          example: Production server
        asset_type:
          type: string
          example: Server
        asset_make:
          type: string
          example: Dell
        asset_model:
          type: string
          example: PowerEdge R650
        asset_serial:
          type: string
          example: ABC123456
        asset_os:
          type: string
          example: Ubuntu 24.04
        asset_ip:
          type: string
          example: 192.168.1.10
        asset_mac:
          type: string
          example: '00:11:22:33:44:55'
        asset_uri:
          type: string
          nullable: true
          example: https://server01.example.com
        asset_uri_2:
          type: string
          nullable: true
          example: ''
        asset_status:
          type: string
          example: Active
        asset_purchase_date:
          type: string
          format: date
          nullable: true
          example: '2026-01-15'
        asset_warranty_expire:
          type: string
          format: date
          nullable: true
          example: '2029-01-15'
        asset_install_date:
          type: string
          format: date
          nullable: true
          example: '2026-01-20'
        asset_notes:
          type: string
          nullable: true
          example: Production server
        asset_vendor_id:
          type: integer
          example: 10
        asset_location_id:
          type: integer
          example: 5
        asset_contact_id:
          type: integer
          example: 25
        asset_network_id:
          type: integer
          example: 3
        asset_client_id:
          type: integer
          example: 1
    CreateAssetRequest:
      type: object
      required:
        - client_id
        - asset_name
      properties:
        client_id:
          type: integer
          example: 1
        asset_name:
          type: string
          example: Server01
        asset_description:
          type: string
          nullable: true
          example: Production server
        asset_type:
          type: string
          example: Server
        asset_make:
          type: string
          example: Dell
        asset_model:
          type: string
          example: PowerEdge R650
        asset_serial:
          type: string
          example: ABC123456
        asset_os:
          type: string
          example: Ubuntu 24.04
        asset_ip:
          type: string
          example: 192.168.1.10
        asset_mac:
          type: string
          example: '00:11:22:33:44:55'
        asset_uri:
          type: string
          nullable: true
          example: https://server01.example.com
        asset_uri_2:
          type: string
          nullable: true
          example: ''
        asset_status:
          type: string
          example: Active
        asset_purchase_date:
          type: string
          format: date
          nullable: true
          example: '2026-01-15'
        asset_warranty_expire:
          type: string
          format: date
          nullable: true
          example: '2029-01-15'
        asset_install_date:
          type: string
          format: date
          nullable: true
          example: '2026-01-20'
        asset_notes:
          type: string
          nullable: true
          example: Production server
        asset_vendor_id:
          type: integer
          example: 10
        asset_location_id:
          type: integer
          example: 5
        asset_contact_id:
          type: integer
          example: 25
        asset_network_id:
          type: integer
          example: 3
    UpdateAssetRequest:
      type: object
      required:
        - client_id
        - asset_id
      properties:
        client_id:
          type: integer
          example: 1
        asset_id:
          type: integer
          example: 123
        asset_name:
          type: string
          example: Server01
        asset_description:
          type: string
          nullable: true
          example: Production server
        asset_type:
          type: string
          example: Server
        asset_make:
          type: string
          example: Dell
        asset_model:
          type: string
          example: PowerEdge R650
        asset_serial:
          type: string
          example: ABC123456
        asset_os:
          type: string
          example: Ubuntu 24.04
        asset_ip:
          type: string
          example: 192.168.1.10
        asset_mac:
          type: string
          example: '00:11:22:33:44:55'
        asset_uri:
          type: string
          nullable: true
          example: https://server01.example.com
        asset_uri_2:
          type: string
          nullable: true
          example: ''
        asset_status:
          type: string
          example: Active
        asset_purchase_date:
          type: string
          format: date
          nullable: true
          example: '2026-01-15'
        asset_warranty_expire:
          type: string
          format: date
          nullable: true
          example: '2029-01-15'
        asset_install_date:
          type: string
          format: date
          nullable: true
          example: '2026-01-20'
        asset_notes:
          type: string
          nullable: true
          example: Production server
        asset_vendor_id:
          type: integer
          example: 10
        asset_location_id:
          type: integer
          example: 5
        asset_contact_id:
          type: integer
          example: 25
        asset_network_id:
          type: integer
          example: 3
    DeleteAssetRequest:
      type: object
      required:
        - client_id
        - asset_id
      properties:
        client_id:
          type: integer
          example: 1
        asset_id:
          type: integer
          example: 123
